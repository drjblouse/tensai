version: '3'

services:
   db:
     image: cnadiminti/dynamodb-local:latest
     ports:
       - "${DB_PORT}:8000"
     volumes:
       - db_data:${PWD}/dynamodb_local_db
     restart: always
     container_name: local_dynamo_db

volumes:
    db_data:
